---
import Hero from '@svg/hero/Hero.astro';
import MobileHero from '@svg/hero/MobileHero.astro';
---

<section id='hero'>
  <MobileHero class='hero-image-mobile' width='100%' height='100%' />
  <Hero class='hidden hero-image' width='100%' height='100%' />
  <div>
    <h1>
      Hi, I'm Catalin <br /> a <strong>Software Developer</strong>
      <br /> & <strong>Electrical Engineer</strong>
    </h1>
    <p class='text-fontLightGray3 dark:text-fontDarkGray2'>
      Passionate about developing applications, excited to explore new
      possibilities and driven to expand my knowledge constantly.
    </p>
    <p class='text-fontLightGray3 dark:text-fontDarkGray2'>
      Let's connect and explore opportunities together, whether you want to
      improve an existing application or build something new.
    </p>
    <a
      class='border-2 border-btnLight bg-btnLight text-btnFont hover:bg-transparent dark:bg-btnDark dark:border-btnDark dark:hover:text-dark'
      href='#projects'
      >Explore&nbsp;projects
    </a>
  </div>
</section>

<style>
  .hero-image {
    width: 100%;
    height: 100%;
  }

  h1 {
    margin-top: 1.6rem;
    font-size: clamp(2.2rem, 6vw, 4.5rem);
    line-height: 1.3;
  }

  p {
    margin-top: 1.6rem;
    display: block;
    max-width: 50ch;
    font-size: clamp(1.6rem, 2.5vw, 2rem);
  }

  a {
    margin-top: 3rem;
    display: inline-block;
    padding: 1rem 2rem;
    font-size: clamp(1.7rem, 2.5vw, 2rem);
    font-weight: 900;
    border-radius: 10px;
    transition: all 200ms ease-in;
  }

  @media only screen and (min-width: 1000px) {
    h1 {
      margin-top: 1.6rem;
      font-size: clamp(3.7rem, 3.4vw, 4.5rem);
      line-height: 1.3;
    }

    .hero-image {
      display: block;
      order: 1;
      max-width: min(550px, 45%);
    }

    .hero-image-mobile {
      display: none;
    }

    section {
      margin-top: 8rem;
      display: flex;
      gap: 3rem;
      justify-content: space-between;
    }

    a {
      margin-top: 5rem;
    }

    @media only screen and (min-width: 400px) {
      a {
        padding: 1.5rem 2.5rem;
      }
    }
  }
</style>
